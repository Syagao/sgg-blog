---
interface Props {
	date: Date;
	showYear?: boolean;
}

const { date, showYear = true } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('zh-CN', {
			year: showYear ? 'numeric' : undefined,
			month: '2-digit',
			day: '2-digit',
		})
	}
</time>
