# 当前状态

更新日期：2026-09-13。

## 已具备

- Astro 静态站点与正式域名配置；
- Markdown/MDX 内容集合和 schema 校验；
- 首页、兼容旧地址的文章列表、文章详情、标签、归档、关于和 404 页面；
- 文章目录、标题锚点、代码复制、阅读时间、标签和前后文章导航；
- RSS、canonical、Open Graph、Twitter 元数据和 sitemap；
- 深浅主题、移动端基础响应式样式和键盘可访问性基础；
- 本地托管的 Inter 与霞鹜文楷屏幕阅读版字体；
- 使用 `pic/jiamo.jpg`、`Shen Gao Gao` 和两行导航结构的响应式页头，页头与正文统一使用 780px 版心；右上角提供 GitHub 主页入口和主题切换按钮；
- 顶部导航使用红色文字标识当前页面；悬停或键盘聚焦时显示红色文字和展开式下划线；
- 顶部和页脚导航使用“首页、归档、关于”作为主要页面入口，页脚额外保留 RSS；“文章”入口已由“归档”完整替代；
- 归档页使用“按年份分组的完整日期文章列表 + 标签索引”双栏布局，窄屏自动改为单栏；
- 首页使用数学基础、运动控制、大模型知识和人形机器人四个静态技术版块，桌面端两列、窄屏单列；
- 首页最新文章区使用“完整日期 + 带下划线标题”的紧凑列表，标题区通过浅色横线连接归档入口；
- 3 篇公开内容样本。

## 正在进行

整体视觉和首页信息结构正在重新设计。首页原有的大型个人开场已经移除，第一阶段四个技术版块与紧凑最新文章列表已经实施；版块暂不设置跳转，待对应内容形成后再设计集合页。D-008 的字体栈与本地字体文件已经实施；“关于”页已经采用 36px / 600 标题和 17px / 400、1.85 行高、24px 段距的正文排版。其余页面的字号层级仍待后续统一。具体要求记录在 `docs/decisions.md`。

## 已知问题与占位内容

- 首页四个技术版块目前是静态展示内容，尚未与文章标签或独立集合页建立关联。
- 仓库含未使用的 Atkinson 字体文件和多张占位图片；其中只有一张占位图被现有 MDX 示例引用。
- `SITE_URL` 常量当前未被代码消费，域名实际由 `astro.config.mjs` 提供。
- 已有基于 Node.js 内置测试运行器的构建产物回归测试，但还没有独立的测试脚本、lint 或格式化脚本。
- 部署平台和发布流程未记录在仓库中。

## 最近验证

2026-09-13 运行 `npm run build` 成功：

- Astro 完成内容同步和类型生成；
- 15 个页面成功构建；
- RSS、标签页和 `sitemap-index.xml` 成功生成。
- 19 项构建产物回归测试通过，覆盖首页版块与紧凑列表、文章入口替换、文章详情栏目归属、归档年份与标签索引、导航交互和 GitHub 按钮。
- 构建产物包含 Inter 与霞鹜文楷的 `@font-face` 声明、101 个 WOFF2 文件（5,199,128 字节），所有字体 URL 均能解析到对应文件，且未发现外部字体服务地址。

开发服务器当时由 Astro 后台模式运行在 `http://localhost:4321`。按照根目录 `AGENTS.md`，后续仍应使用 `astro dev --background` 以及对应的 `status`、`logs`、`stop` 命令管理。

## 建议的近期顺序

1. 按 D-009 实现并验证字号和行高，再继续确认颜色与版心。
2. 明确首页模块的含义、分组和点击行为。
3. 确定首页与导航线框。
4. 实现首页和通用视觉令牌。
5. 统一文章列表、文章页、归档与关于页。
6. 替换个人资料和示例内容。
7. 增加必要的自动化检查并验证生产发布。
